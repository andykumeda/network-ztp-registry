# Demo Ansible Content

This directory contains sanitized example playbooks used by the dashboard's
playbook runner. Replace the inventory and encrypted variables before adapting
the project to a real lab.
